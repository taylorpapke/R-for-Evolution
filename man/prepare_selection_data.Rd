% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prepare_selection_data.R
\name{prepare_selection_data}
\alias{prepare_selection_data}
\title{Prepare Data for Selection Analysis}
\usage{
prepare_selection_data(
  data,
  fitness_col,
  trait_cols,
  standardize = TRUE,
  add_relative = TRUE,
  na_action = c("warn", "drop", "none"),
  name_relative = "relative_fitness"
)
}
\arguments{
\item{data}{A \code{data.frame}.}

\item{fitness_col}{Character. Name of the fitness column (0/1 or continuous).}

\item{trait_cols}{Character vector. Names of trait columns to standardize.}

\item{standardize}{Logical; whether to z-score traits. Default \code{TRUE}.}

\item{add_relative}{Logical; whether to add a relative-fitness column.}

\item{na_action}{One of \code{"warn"}, \code{"drop"}, \code{"none"}.
\itemize{
  \item \code{"warn"}: keep rows, emit warnings (default)
  \item \code{"drop"}: drop rows with NA in fitness or traits
  \item \code{"none"}: keep rows silently
}}

\item{name_relative}{Character. Name of the relative-fitness column to add.}
}
\value{
A \code{data.frame} with standardized traits (if requested) and,
        optionally, a relative-fitness column.
}
\description{
Standardize trait columns (z-score) if requested, and (optionally) add
relative fitness. Flexible NA handling with warnings/drop/none.
}
\examples{
set.seed(1)
df <- data.frame(fitness = rbinom(10, 1, 0.4),
                 size = rnorm(10, 5, 1),
                 speed = rnorm(10, 10, 2))
out <- prepare_selection_data(df, "fitness", c("size", "speed"),
                              standardize = TRUE,
                              add_relative = TRUE,
                              na_action = "warn")
head(out)
}
